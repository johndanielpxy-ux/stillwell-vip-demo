import {createApp} from '../server.mjs';
const env={OPENAI_API_KEY:'local-mock-only',DEMO_ACCESS_CODE:'ux-test',OPENAI_MODEL:'UX_TEST_DOUBLE'};
createApp({env,fetchImpl:async(url,options)=>{const input=JSON.parse(options.body);await new Promise(r=>setTimeout(r,700));let output;if(input.text.format.name==='stillwell_document'){output={title:'Mock extraction',date:'2026-09-13',summary:'Test-double report for UX verification.',fields:[{label:'Haemoglobin',value:'12.8',unit:'g/dL',page:null,quote:'Haemoglobin: 12.8 g/dL'}],uncertainties:[]};}else{const req=JSON.parse(input.input);const source=req.sources.find(s=>s.id.startsWith('record:'));output={message:'Test-double answer from your selected report.',citations:source?[{sourceId:source.id,quote:source.content.slice(0,60)}]:[],observation:null,questions:[]};}return {ok:true,json:async()=>({status:'completed',output:[{content:[{type:'output_text',text:JSON.stringify(output)}]}]})};}}).listen(4319,'127.0.0.1',()=>console.log('UX test double on 4319; no real provider calls'));
